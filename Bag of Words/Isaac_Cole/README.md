Explores sentence embeddings of sentences that consist of the same words but different order.

Uses pretrained word embeddings for hopefully decent relations.

Initially compares sentence embeddings generated from averaging words before experimenting with methods to account for word order.