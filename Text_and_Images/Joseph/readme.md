**Image Generation Project

*processing_clip.ipynb

Very simple project (honestly too simple) where I attempted to hyperparameter tune the guidance_scale parameter (which implements classifier free guidance). I tried to do some type of similarity score using the same clip model. Unfortunately this failed and I'll have to try something else sometime. 
